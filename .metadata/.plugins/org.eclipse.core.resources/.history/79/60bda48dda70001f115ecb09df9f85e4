package com.crimsonlogic.groceriessubbookingsystem.dto;

import com.crimsonlogic.groceriessubbookingsystem.entity.Order;
import lombok.Data;

@Data
public class OrderDetailsDTO {
    private String orderId;
    private String userId;
    private String groceryId;
    private String groceryName;
    private float groceryPrice;
    private float totalAmount;
    private Order.OrderStatus orderStatus;
    private String orderDate;

    public static OrderDetailsDTO fromOrder(Order order) {
        OrderDetailsDTO dto = new OrderDetailsDTO();
        dto.setOrderId(order.getOrder_id());
        dto.setUserId(order.getUsers().getUserId()); // Assuming getUserId() exists in Users
        dto.setGroceryId(order.getGrocery().getGroceryId()); // Assuming getGroceryId() exists in Groceries
        dto.setGroceryName(order.getGrocery().getName()); // Assuming getName() exists in Groceries
        dto.setGroceryPrice(order.getGrocery().getPrice()); // Assuming getPrice() exists in Groceries
        dto.setTotalAmount(order.getTotalAmount());
        dto.setOrderStatus(order.getOrderStatus());
        dto.setOrderDate(order.getOrder_date().toString());
        return dto;
    }
}
